# HTML to BB Converter for VS Code

Конвертирует выделенный HTML в BB-код для форумов MyBB/RusFF.

## Возможности

### 🔄 Конвертация HTML → BB

| HTML | BB |
|------|-----|
| `<div class="name">` | `[block=name]` |
| `<h1>` - `<h6>` | `[block=bh1]` - `[block=bh6]` |
| `<b>`, `<strong>` | `[b]` |
| `<i>`, `<em>` | `[i]` |
| `<u>` | `[u]` |
| `<s>`, `<del>` | `[s]` |
| `<a href="...">` | `[url=...]` (с `[abbr]` если есть title) |
| `<img src="...">` | `[img]...[/img]` (с `[abbr]` если есть title) |
| `<ul>`, `<ol>` | `[ul]`, `[ol]` |
| `<table>` | `[table]` |
| `<tr>` | `[tr]` |
| `<td>` | `[td]` (с сохранением `colspan`/`rowspan`) |
| `<th>` | `[td]` (с сохранением `colspan`/`rowspan`) |
| `<blockquote>` | `[quote]` |
| `<div class="quote-box answer-box">` | `[quote="автор"]` |
| `<div class="quote-box hide-box">` | `[hide=число]` |
| `<details><summary>...</summary>...</details>` | `[spoiler="..."]...[/spoiler]` |
| `<hr>` | `[hr]` |

### 🎨 Подсветка синтаксиса BBCode
- Файлы `.bb` и `.bbcode` автоматически подсвечиваются
- Разные цвета для тегов, атрибутов и текста

### 📝 Сниппеты
Быстрая вставка BB-тегов: `block`, `url`, `img`, `b`, `i`, `u`, `s`, `table`, `tr`, `td`, `quote`, `spoiler`, `hide` и другие.

### ✨ Форматирование
- Очистка от лишних пробелов и пустых строк
- Сохранение структуры
- Поддержка форматирования всего документа и выделенного фрагмента

### 🖱️ Удобство
- Контекстное меню (правый клик) → "BB Converter: Конвертировать выделенный HTML"
- Горячие клавиши: `Ctrl+Shift+B` (настраивается)

## Установка

- Скачайте `.vsix` файл из [релизов](https://github.com/alexkolmar/html-to-bb-converter/releases)
- В VS Code: Extensions → ... → Install from VSIX
- Выберите скачанный файл

Или установите прямо из [Visual Studio Marketplace](https://marketplace.visualstudio.com/items?itemName=AlexKolmar.html-to-bb-converter).

## Использование

1. Выделите HTML код
2. Нажмите правую кнопку мыши
3. Выберите "BB Converter: Конвертировать выделенный HTML"

Для форматирования BBCode:
- Правый клик → "Format Document" или `Shift+Alt+F`
- Правый клик → "Format Selection" или `Ctrl+K Ctrl+F`

## Лицензия

MIT

## Автор

**Alex Kolmar**